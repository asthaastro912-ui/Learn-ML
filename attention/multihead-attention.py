"""
If one attention head can attend to every token, why do we need multiple heads?
"A single attention head learns one pattern of relationships between tokens.
 Multiple heads allow the model to learn different types of relationships 
 simultaneously, such as syntax, semantics, positional dependencies, or 
 long-range interactions. The outputs are then concatenated, giving the model 
 a richer representation than a single head could provide."


"""